# Site settings
title: "dry-labs"
description: dry-labs company

baseurl: "" # the subpath of your site, e.g. /blog
url: "https://dry-labs.github.io" # the base hostname & protocol for your site

# Build settings
include: [_pages]
markdown: kramdown
permalink: /:year/:month/:title

sass:
  style: compressed

exclude: [vendor]

# Plugins:
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-feed
